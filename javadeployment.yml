apiVersion: apps/v1
kind: Deployment
metadata:
  name: javadeployment
  namespace: flipkart
spec:
  replicas: 2
  selector:
    matchLabels:
      app: javaweb
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 30
  template:
    metadata:
      name: javapod
      namespace: flipkart
      labels:
        app: javaweb
    spec:
      imagePullSecrets:
      - name: dockerreposecret
      containers:
        name: javacontainer
        image: 172.31.11.149:8084/javawebapp:6
        ports:
        - containerPort: 9090
---

apiVersion: v1
kind: Service
metadata:
  name: javaservice
  namespace: flipkart
spec:
  type: NodePort
  selector:
    app: javaweb
  ports:
  - port: 80
    targetPort: 9090
